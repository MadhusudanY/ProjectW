# This is a sample Python script.
import re

import processWord

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.


if __name__ == '__main__':
    processWord.execute()
    # sample_str = "	This work is performed indoors Page 113 of 1031 and out-of-doors, and includes night and
    # weekend  " \ "duties Page 2 of 5" sample_str = re.sub(r'Page [0-9]+ of [0-9]+', '', sample_str) print(f'"{
    # sample_str}"')
